openapi: 3.1.0
info:
  title: GeoLife Home / Office API
  description: HTTP serving layer for the frozen CP2 v1 Home / Office heuristic baseline. Evidence strength is not a calibrated probability.
  version: 1.0.0
paths:
  /health:
    get:
      tags:
        - system
      summary: Health
      operationId: health_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /v1/home-office/infer:
    post:
      tags:
        - inference
      summary: Infer Home / Office evidence from one user's CP1 stay events
      operationId: infer_home_office_endpoint_v1_home_office_infer_post
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InferRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InferResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
components:
  schemas:
    AbstainedResult:
      type: object
      additionalProperties: false
      required: [label, reason]
      properties:
        label:
          type: string
          enum: [HOME, OFFICE]
          title: Label
        status:
          type: string
          const: abstained
          default: abstained
          title: Status
        reason:
          $ref: '#/components/schemas/AbstentionReason'
      title: AbstainedResult
    AbstentionReason:
      type: string
      enum:
        - out_of_scope_geography
        - insufficient_recurring_history
        - insufficient_semantic_evidence
      title: AbstentionReason
    EmittedResult:
      type: object
      additionalProperties: false
      required:
        - label
        - location_id
        - evidence_strength
        - relevant_dwell_share
        - share_margin
        - relevant_dates
        - relevant_dwell_h
      properties:
        label:
          type: string
          enum: [HOME, OFFICE]
          title: Label
        status:
          type: string
          const: emitted
          default: emitted
          title: Status
        location_id:
          type: integer
          minimum: 0
          title: Location Id
        evidence_strength:
          type: number
          minimum: 0
          maximum: 1
          title: Evidence Strength
        relevant_dwell_share:
          type: number
          minimum: 0
          maximum: 1
          title: Relevant Dwell Share
        share_margin:
          type: number
          minimum: 0
          maximum: 1
          title: Share Margin
        relevant_dates:
          type: integer
          minimum: 0
          title: Relevant Dates
        relevant_dwell_h:
          type: number
          minimum: 0
          title: Relevant Dwell H
      title: EmittedResult
    HealthResponse:
      type: object
      additionalProperties: false
      properties:
        status:
          type: string
          const: ok
          default: ok
          title: Status
        service:
          type: string
          const: geolife-home-office-api
          default: geolife-home-office-api
          title: Service
        api_version:
          type: string
          const: v1
          default: v1
          title: Api Version
        model_contract:
          type: string
          const: cp2-v1
          default: cp2-v1
          title: Model Contract
      title: HealthResponse
    InferRequest:
      type: object
      additionalProperties: false
      required: [user_id, stays]
      properties:
        user_id:
          type: string
          minLength: 1
          maxLength: 256
          title: User Id
        stays:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/StayEvent'
          title: Stays
      title: InferRequest
    InferResponse:
      type: object
      additionalProperties: false
      required: [user_id, results]
      properties:
        user_id:
          type: string
          title: User Id
        model_contract:
          type: string
          const: cp2-v1
          default: cp2-v1
          title: Model Contract
        results:
          type: array
          minItems: 2
          maxItems: 2
          items:
            oneOf:
              - $ref: '#/components/schemas/EmittedResult'
              - $ref: '#/components/schemas/AbstainedResult'
            discriminator:
              propertyName: status
              mapping:
                emitted: '#/components/schemas/EmittedResult'
                abstained: '#/components/schemas/AbstainedResult'
          title: Results
      title: InferResponse
    StayEvent:
      type: object
      additionalProperties: false
      required: [arrival_time_utc, departure_time_utc, latitude, longitude]
      properties:
        arrival_time_utc:
          type: string
          format: date-time
          title: Arrival Time Utc
        departure_time_utc:
          type: string
          format: date-time
          title: Departure Time Utc
        latitude:
          type: number
          minimum: -90
          maximum: 90
          title: Latitude
        longitude:
          type: number
          minimum: -180
          maximum: 180
          title: Longitude
      title: StayEvent
    HTTPValidationError:
      type: object
      properties:
        detail:
          type: array
          items:
            $ref: '#/components/schemas/ValidationError'
          title: Detail
      title: HTTPValidationError
    ValidationError:
      type: object
      required: [loc, msg, type]
      properties:
        loc:
          type: array
          items:
            anyOf:
              - type: string
              - type: integer
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      title: ValidationError
