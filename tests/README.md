# Tests

Production code for data cleaning, geospatial distance, stay-point detection, and classification will be implemented test-first (RED → GREEN).

The current CP1 EDA helper code remains under `notebooks/` and is exploratory; reviewed logic can later be promoted into `src/geolife/` with unit tests.
