# EDA reports

`generated/` is ignored by Git by default.

After running the EDA, review outputs for licensing/privacy concerns before publishing anything. Prefer written aggregate findings over raw or user-level trajectory artifacts.
